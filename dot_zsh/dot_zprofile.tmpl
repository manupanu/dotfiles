{{- $os := .chezmoi.os }}
{{- if hasKey . "targetOS" }}
{{- $os = .targetOS }}
{{- end }}
{{- if eq $os "darwin" }}
# Homebrew (macOS)
if [[ -x "/opt/homebrew/bin/brew" ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv zsh)"
elif [[ -x "/usr/local/bin/brew" ]]; then
  eval "$(/usr/local/bin/brew shellenv zsh)"
fi
{{- end }}

# Editor & env
export EDITOR="code --reuse-window --wait"
export VISUAL="code --reuse-window --wait"
export PIPENV_VENV_IN_PROJECT=1
export BRAVE_SEARCH_API_KEY={{ onepasswordRead "op://Private/Brave Search/credential" }}
export SERPER_API_KEY={{ onepasswordRead "op://Private/Serper API Key/credential" }}

# PATH
[[ -d "$HOME/bin" ]] && PATH="$HOME/bin:$PATH"
[[ -d "$HOME/.local/bin" ]] && PATH="$HOME/.local/bin:$PATH"
{{- if eq $os "linux" }}
[[ -d "/usr/local/bin" ]] && PATH="/usr/local/bin:$PATH"
{{- end }}
export PATH
